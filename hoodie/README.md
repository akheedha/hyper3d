# TIMED COLORS — Hoodie Category: render the baked hoodie in VS Code

This folder contains a complete mainImage shader and the actual atlas from your
128³ SDF bake for the Hyper3D Hoodie. It ray-marches your hoodie volume. No GLB loader
or new mesh conversion is needed at runtime.

## Files in this Folder

| File | Type | Purpose |
|---|---|---|
| [`TC_Hoodie_SDF.glsl`](file:///d:/HYPER3D/hyper3D/hoodie/TC_Hoodie_SDF.glsl) | GLSL Shader | The main real-time raymarching shader for the VS Code Shader Toy extension. Marches rays through the bounding box, samples the atlas, calculates surface normals, and renders lighting and debug modes. |
| [`hoodie_sdf_128_atlas.png`](file:///d:/HYPER3D/hyper3D/hoodie/hoodie_sdf_128_atlas.png) | Image (PNG) | 2048 × 1024 RGBA8 2D texture atlas storing the 128³ distance field as 16 × 8 tiles (128 × 128 slices each). Encodes 16-bit distances in Red (high byte) and Green (low byte) channels. |
| [`hoodie_sdf_128.json`](file:///d:/HYPER3D/hyper3D/hoodie/hoodie_sdf_128.json) | Metadata (JSON) | Specification file containing grid resolution (128³), 3D bounding box coordinates (`TC_MIN`, `TC_MAX`), voxel size ($h$), distance range, tile layout, and probe pixel byte values. |
| [`hoodie_sdf_128.f32`](file:///d:/HYPER3D/hyper3D/hoodie/hoodie_sdf_128.f32) | Binary Data | 8 MB raw IEEE 754 float32 distance grid in little-endian order (`128 × 128 × 128` values) used as ground-truth numerical reference for validation. |
| [`Hoodie.glb`](file:///d:/HYPER3D/hyper3D/hoodie/Hoodie.glb) | 3D Model | The original 3D mesh file in binary glTF format (19,925 vertices, 32,558 triangles) from which the SDF was baked. |
| [`check_atlas.py`](file:///d:/HYPER3D/hyper3D/hoodie/check_atlas.py) | Python Script | Verification script that validates atlas decoding against `hoodie_sdf_128.f32`, tests probe orientation checks, simulated sRGB rejection, and performs 4-view CPU reference raymarching. |
| [`checks.json`](file:///d:/HYPER3D/hyper3D/hoodie/checks.json) | Test Results (JSON) | Recorded numerical verification results produced by `check_atlas.py` (roundtrip error, orientation status, hit pixel counts, and capped ray stats). |
| [`CPU_Reference.png`](file:///d:/HYPER3D/hyper3D/hoodie/CPU_Reference.png) | Image (PNG) | 4-view CPU reference render (FRONT 0°, SIDE 90°, BACK 180°, OTHER SIDE 270°) generated by `check_atlas.py` to compare against GPU output. |
| [`bake_hoodie_sdf.py`](file:///d:/HYPER3D/hyper3D/hoodie/bake_hoodie_sdf.py) | Python Script | The baking pipeline script that reads `Hoodie.glb`, computes the 128³ distance volume, encodes RG16 texels, and generates `hoodie_sdf_128_atlas.png`, `hoodie_sdf_128.json`, and `hoodie_sdf_128.f32`. |
| [`verify_hoodie_preview.mjs`](file:///d:/HYPER3D/hyper3D/hoodie/verify_hoodie_preview.mjs) | Node.js Script | Test harness that generates a local Three.js / WebGL HTML page (`tc_hoodie_preview.html`) to verify shader compilation and measure real-time FPS/frame times. |
| [`SHA256.json`](file:///d:/HYPER3D/hyper3D/hoodie/SHA256.json) | Checksums (JSON) | Cryptographic SHA-256 hashes of all assets in this folder to ensure file integrity. |
| [`README.md`](file:///d:/HYPER3D/hyper3D/hoodie/README.md) | Documentation | This category guide, detailing usage instructions, uniform controls, contracts, and troubleshooting. |

---

## Run in VS Code

1. In VS Code, open this workspace.
2. Keep `TC_Hoodie_SDF.glsl` and `hoodie_sdf_128_atlas.png` in this folder.
3. Open `TC_Hoodie_SDF.glsl`.
4. With `jakearl.shader-toy-web` installed, run **Shader Toy: Show GLSL Preview**
   from the Command Palette (`Ctrl+Shift+P`). **Show Static GLSL Preview** also works and keeps
   the preview attached when you open another editor.
5. Start with a small preview pane, approximately 500 × 650 pixels. The initial
   view is stationary and front-facing. Compilation can take a moment.
6. Use the extension's custom-uniform controls. If those controls are not visible,
   edit the defaults in the `#iUniform` lines at the top of the shader and save.

| Control | Values / purpose |
|---|---|
| viewAngle | 0 front; 90 side; 180 back; 270 other side |
| autoRotate | 0 stationary; 1 turntable |
| rotationSpeed | Degrees per second; default 12 |
| zoom | Default 1; reduce if cropped |
| debugMode | 0 hoodie; 1 raw atlas and green validation strip; 2 distance slice |
| sliceZ | 0–127; used only in distance-slice mode |

The first expected result is a light gray/white hoodie with directional shading on a plain gray background.

---

## Texture Contract

- PNG is numerical data, not a diffuse/color map.
- 2048 × 1024 RGBA8, 16 × 8 tiles, each tile 128 × 128. Tiles are ordered by Z.
- R is the high byte and G the low byte of an unsigned 16-bit value.
- Decode `distance = ((Rbyte*256 + Gbyte)/65535*2 - 1)*1.230000`.
- The shader fetches texel centers with nearest filtering and interpolates decoded
  distances manually. Do not enable sRGB conversion or mipmap minification.
- It detects the uploaded image's vertical orientation using three known byte
  pairs; either upload flip convention works.
- Bounds, cell-center convention and units are in `hoodie_sdf_128.json`.
- The original model uses +Y up and +Z front.

---

## Troubleshooting

**Red checkerboard:** image loading or byte validation failed. Confirm the folder
is open, the relative path is correct and the exact PNG is present. Wait for loading,
then reopen the preview. Check extension error messages.

**Magenta pixels:** rays reached the step limit. The step cap is set to 1000 for
fine cloth folds.

**GLSL error:** copy the complete error text and line to Codex. The custom `#iChannel`
and `#iUniform` lines are extension directives, not standard GLSL.

**Slow preview:** turn autoRotate off, reduce the pane size, and keep only one
preview open.

---

## Validation and Scope

`check_atlas.py` independently checks the atlas against the float32 volume,
tests orientation calibration, and makes four CPU reference renders using the
same sampling/camera/marching rules. See `checks.json` for the measured results
and `CPU_Reference.png` for the reference.

Official extension documentation:
https://marketplace.visualstudio.com/items?itemName=jakearl.shader-toy-web
